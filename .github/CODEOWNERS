# CODEOWNERS — review routing.
#
# This is single-operator by default. If you bring on a second operator,
# split per-area (e.g. terraform → @ops, docs → @docs).
#
# CODEOWNERS currently routes review but is not a required gate while the
# repository has one maintainer. See docs/BRANCH-PROTECTION.md.

* @po4yka

# Specific area owners — add as the team grows
# /terraform/        @po4yka @<infra-operator>
# /ansible/          @po4yka
# /docs/             @po4yka @<docs-owner>
# /.github/          @po4yka
