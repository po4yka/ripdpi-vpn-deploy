---
name: ci

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gitleaks:
    name: gitleaks
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Run gitleaks with repo rules
        uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e  # v3.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_CONFIG: ./.gitleaks.toml
          GITLEAKS_NOTIFY_USER_LIST: ""
          GITLEAKS_ENABLE_UPLOAD_ARTIFACT: "false"
          GITLEAKS_ENABLE_COMMENTS: "false"

  actionlint:
    name: actionlint
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - name: Install actionlint
        run: go install github.com/rhysd/actionlint/cmd/actionlint@v1.7.7
      - name: Lint workflows
        run: |
          actionlint_path="$(go env GOPATH)/bin/actionlint"
          "$actionlint_path"

  zizmor:
    name: zizmor
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: read
    env:
      ZIZMOR_VERSION: "1.29.0"
      ZIZMOR_SHA256: dd96df044a6e8538d5f423790f453bdd03d49e5b2bcc38214acc41a2f1297839
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Install checksum-verified zizmor
        run: |
          set -euo pipefail
          archive="$RUNNER_TEMP/zizmor.tar.gz"
          bin_dir="$RUNNER_TEMP/zizmor-bin"
          curl -fsSL \
            "https://github.com/zizmorcore/zizmor/releases/download/v${ZIZMOR_VERSION}/zizmor-x86_64-unknown-linux-gnu.tar.gz" \
            -o "$archive"
          echo "${ZIZMOR_SHA256}  ${archive}" | sha256sum -c -
          mkdir -p "$bin_dir"
          tar -xzf "$archive" -C "$bin_dir"
          echo "$bin_dir" >> "$GITHUB_PATH"

      - name: Audit owned GitHub automation
        run: make zizmor-check ZIZMOR_FORMAT=github

      - name: Verify fail-closed scope and findings
        run: make zizmor-test

  task-contract:
    name: task-contract
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    env:
      OPENSPEC_TELEMETRY: "0"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: tools/tasking/package-lock.json

      - name: Install pinned tasking tools
        run: npm ci --prefix tools/tasking --ignore-scripts

      - name: Validate local task contracts
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
        run: |
          args=()
          base_sha=""
          if [[ "$EVENT_NAME" == "pull_request" && -n "$PR_BASE_SHA" ]]; then
            base_sha="$PR_BASE_SHA"
          elif [[ "$EVENT_NAME" == "push" && "$PUSH_BEFORE_SHA" != "0000000000000000000000000000000000000000" ]]; then
            base_sha="$PUSH_BEFORE_SHA"
          fi
          if [[ -n "$base_sha" ]]; then
            git cat-file -e "${base_sha}^{commit}" || {
              echo "::error::Task-contract base commit is unavailable: $base_sha"
              exit 1
            }
            if git cat-file -e "${base_sha}:tools/tasking/project.json"; then
              args+=(--base "$base_sha")
            else
              echo "::notice::Pre-contract bootstrap base; strict history starts with the next change"
            fi
          fi
          ./taskctl validate "${args[@]}"

  terraform:
    name: terraform fmt + validate
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        provider: [upcloud, hetzner, vultr, scaleway]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e  # v4
        with:
          terraform_version: "1.15.2"

      - name: terraform fmt -check
        working-directory: terraform/providers/${{ matrix.provider }}
        run: terraform fmt -check -recursive

      - name: terraform init -backend=false
        working-directory: terraform/providers/${{ matrix.provider }}
        run: terraform init -backend=false

      - name: terraform validate
        working-directory: terraform/providers/${{ matrix.provider }}
        run: terraform validate

  terraform-exception:
    name: terraform inert exception root
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e  # v4
        with:
          terraform_version: "1.15.2"
      - name: terraform fmt -check
        working-directory: terraform/exception/cascade-ingress
        run: terraform fmt -check
      - name: terraform init without backend
        working-directory: terraform/exception/cascade-ingress
        run: terraform init -backend=false
      - name: terraform validate
        working-directory: terraform/exception/cascade-ingress
        run: terraform validate

  cloud-init:
    name: cloud-init schema
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Install cloud-init
        run: |
          sudo apt-get update
          sudo apt-get install -y cloud-init

      - name: Render cloud-init template
        run: python3 scripts/render-cloud-init-ci.py > /tmp/cloud-init.rendered.yaml

      - name: Validate with cloud-init schema
        run: cloud-init schema --config-file /tmp/cloud-init.rendered.yaml

  ansible:
    name: ansible-lint + syntax-check
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"

      - name: Install pinned tooling
        run: |
          pip install --require-hashes --no-deps -r requirements.txt
          for attempt in 1 2 3; do
            ansible-galaxy collection install -r requirements.yml && break
            if [ "${attempt}" -eq 3 ]; then
              exit 1
            fi
            sleep $((attempt * 10))
          done

      - name: ansible-lint
        working-directory: ansible
        env:
          ANSIBLE_CONFIG: ./ansible.cfg
        run: ansible-lint --offline

      - name: ansible-playbook --syntax-check
        working-directory: ansible
        env:
          ANSIBLE_CONFIG: ./ansible.cfg
          # site.yml asserts VPN_SECRETS_FILE is set; provide a stub for syntax-check.
          VPN_SECRETS_FILE: ${{ github.workspace }}/secrets/prod.secrets.example.yaml
        run: ansible-playbook playbooks/site.yml --syntax-check -i 'localhost,'

  molecule:
    name: molecule (${{ matrix.role }})
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        role:
          - baseline
          - firewall
          - xray
          - hysteria
          - nginx-xhttp
          - watchdog
          - monitoring
          - backup
          - subscription-host
          - amneziawg
          - geodata
          - cascade-egress
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"

      - name: Install pinned tooling
        run: |
          pip install --require-hashes --no-deps -r requirements.txt
          for attempt in 1 2 3; do
            ansible-galaxy collection install -r requirements.yml && break
            if [ "${attempt}" -eq 3 ]; then
              exit 1
            fi
            sleep $((attempt * 10))
          done

      - name: molecule test
        working-directory: ansible/roles/${{ matrix.role }}
        run: molecule test
        env:
          PY_COLORS: "1"
          ANSIBLE_FORCE_COLOR: "1"

  shellcheck:
    name: shellcheck
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - name: Run shellcheck on every shell script
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck
          shellcheck -s bash -S warning scripts/*.sh terraform/exception/cascade-ingress/*.sh

  secrets-coverage:
    name: secrets-coverage
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Install deps
        run: pip install --require-hashes --no-deps -r requirements.txt
      - name: check-secrets-coverage
        run: python3 scripts/check-secrets-coverage.py

  templates-render:
    name: templates-render
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Install deps
        run: |
          pip install --require-hashes --no-deps -r requirements.txt
          sudo apt-get update
          sudo apt-get install -y nginx-light
      - name: Install pinned Xray semantic validator
        run: |
          XRAY_VERSION="$(python3 -c 'import yaml; print(yaml.safe_load(open("secrets/prod.secrets.example.yaml"))["xray"]["version"])')"
          XRAY_DIR="${RUNNER_TEMP}/xray"
          mkdir -p "${XRAY_DIR}"
          curl -fsSL -o "${XRAY_DIR}/xray.zip" "https://github.com/XTLS/Xray-core/releases/download/${XRAY_VERSION}/Xray-linux-64.zip"
          unzip -q "${XRAY_DIR}/xray.zip" -d "${XRAY_DIR}"
          "${XRAY_DIR}/xray" version
          echo "${XRAY_DIR}" >> "${GITHUB_PATH}"
      - name: check-templates-render
        run: python3 scripts/check-templates-render.py
      - name: check-xray-breaking-changes
        run: python3 scripts/check-xray-breaking-changes.py

  yaml:
    name: yamllint
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Install yamllint
        run: pip install --require-hashes --no-deps -r requirements.txt
      - name: yamllint
        run: yamllint -c .yamllint.yml .

  unit-tests:
    name: pytest unit tests
    runs-on: ubuntu-24.04
    env:
      SING_BOX_CLIENT_VERSION: "1.13.16"
      SING_BOX_CLIENT_LINUX_AMD64_SHA256: "e37c312859dfa84cba148f41072ff6369f08361ae91d622dc1fd3aab49611a8d"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - name: Install SOPS and age
        run: |
          sudo apt-get update
          sudo apt-get install -y age
          curl -fsSL "https://github.com/getsops/sops/releases/download/v3.9.0/sops-v3.9.0.linux.amd64" -o /tmp/sops
          echo "0d65660fbe785647ff4f1764d7f69edf598f79d6d79ebbef4a501909b6ff6b82  /tmp/sops" | sha256sum -c -
          sudo install -m 0755 /tmp/sops /usr/local/bin/sops
      - name: Install gitleaks
        run: |
          go install github.com/zricethezav/gitleaks/v8@v8.30.1
          echo "$(go env GOPATH)/bin" >> "${GITHUB_PATH}"
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Install pinned tooling
        run: pip install --require-hashes --no-deps -r requirements.txt
      - name: Install pinned sing-box client parser
        run: |
          version="${SING_BOX_CLIENT_VERSION}"
          archive="sing-box-${version}-linux-amd64.tar.gz"
          curl -fsSL "https://github.com/SagerNet/sing-box/releases/download/v${version}/${archive}" -o "/tmp/${archive}"
          echo "${SING_BOX_CLIENT_LINUX_AMD64_SHA256}  /tmp/${archive}" | sha256sum -c -
          tar -xzf "/tmp/${archive}" -C /tmp
          sudo install -m 0755 "/tmp/sing-box-${version}-linux-amd64/sing-box" /usr/local/bin/sing-box
      - name: Run real pinned sing-box check
        run: python3 scripts/check-singbox-client-compatibility.py --expected-version "${SING_BOX_CLIENT_VERSION}"
      - name: pytest tests/unit/
        run: python3 -m pytest tests/unit/ -v

  bats-test:
    name: bats shell tests
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - name: Install bats
        run: sudo apt-get update && sudo apt-get install -y bats ssss
      - name: bats tests/bats/
        run: bats tests/bats/

  snapshot:
    name: jinja snapshot diff
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Install pinned tooling
        run: pip install --require-hashes --no-deps -r requirements.txt
      - name: render-snapshots check
        run: python3 scripts/render-snapshots.py

  validate-schema:
    name: secrets schema (lenient on example)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Install pinned tooling
        run: pip install --require-hashes --no-deps -r requirements.txt
      - name: validate-secrets (lenient on example schema)
        run: python3 scripts/validate-secrets.py
      - name: validate-bundle (ripdpi contract vs client)
        run: python3 scripts/validate-bundle.py

  tf-test:
    name: terraform test (${{ matrix.provider }})
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        provider: [upcloud, hetzner, vultr, scaleway]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e  # v4
        with:
          terraform_version: "1.15.2"
      - name: terraform init -backend=false
        working-directory: terraform/providers/${{ matrix.provider }}
        run: terraform init -backend=false
      - name: terraform test
        working-directory: terraform/providers/${{ matrix.provider }}
        run: terraform test

  vpnd-test:
    name: vpnd cargo test
    uses: ./.github/workflows/_rust.yml
    with:
      target: x86_64-unknown-linux-gnu
      cargo-command: test --release --locked

  vpnd-clippy:
    name: vpnd cargo clippy
    uses: ./.github/workflows/_rust.yml
    with:
      target: x86_64-unknown-linux-gnu
      cargo-command: clippy --release --all-targets --locked -- -D warnings

  vpnd-msrv:
    name: vpnd MSRV check
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772  # stable
        with:
          toolchain: "1.88.0"
      - run: cargo +1.88.0 check --locked
        working-directory: vpnd

  vpnd-deny:
    name: vpnd dependency policy
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: EmbarkStudios/cargo-deny-action@3c6349835b2b7b196a839186cb8b78e02f7b5f25  # v2.1.1
        with:
          manifest-path: vpnd/Cargo.toml
          arguments: "--all-features --config vpnd/deny.toml"
          rust-version: "1.88.0"

  molecule-failure-scenarios:
    # Non-default Molecule scenarios that exercise red-path / failure
    # injection plus the paired cascade classifier scenarios. Both populated
    # and forced-empty are required; happy-path alone is not promotion evidence.
    name: molecule failure (${{ matrix.role }})
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - role: watchdog
            scenario: failure
          - role: cascade-ingress
            scenario: populated
          - role: cascade-ingress
            scenario: forced-empty
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.12"
      - name: Install pinned tooling
        run: |
          pip install --require-hashes --no-deps -r requirements.txt
          for attempt in 1 2 3; do
            ansible-galaxy collection install -r requirements.yml && break
            if [ "${attempt}" -eq 3 ]; then
              exit 1
            fi
            sleep $((attempt * 10))
          done
      - name: molecule test -s ${{ matrix.scenario }}
        working-directory: ansible/roles/${{ matrix.role }}
        run: molecule test -s ${{ matrix.scenario }}
        env:
          PY_COLORS: "1"
          ANSIBLE_FORCE_COLOR: "1"

  required:
    name: required checks
    if: always()
    needs:
      - gitleaks
      - actionlint
      - zizmor
      - task-contract
      - terraform
      - terraform-exception
      - cloud-init
      - ansible
      - molecule
      - shellcheck
      - secrets-coverage
      - templates-render
      - yaml
      - unit-tests
      - bats-test
      - snapshot
      - validate-schema
      - tf-test
      - vpnd-test
      - vpnd-clippy
      - vpnd-msrv
      - vpnd-deny
      - molecule-failure-scenarios
    runs-on: ubuntu-24.04
    steps:
      - env:
          RESULTS: ${{ toJSON(needs.*.result) }}
        run: jq -e 'all(.[]; . == "success")' <<< "$RESULTS"
