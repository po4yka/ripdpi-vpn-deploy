---
- name: Bind the isolated published-stack controller adapter
  ansible.builtin.import_playbook: ../controller-converge-adapter.yml

- name: Converge — full site.yml against a docker container (published-ports variant)
  ansible.builtin.import_playbook: ../../playbooks/site.yml
