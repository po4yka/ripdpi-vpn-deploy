---
- name: Reload observability dead-man systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart observability dead-man
  ansible.builtin.systemd_service:
    name: observability-deadman
    enabled: true
    state: restarted
  when: not ansible_check_mode
