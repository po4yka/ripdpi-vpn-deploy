# CIC-1787463116104727: Harden CI tooling installs and extend test coverage

## Objective

CI supply-chain installs are checksum-verified, the full-stack molecule scenario gates every PR, renovate covers build-tool pins without touching staged transport rotation, vpnd reads YAML through a maintained crate, and deploy-gating paths gained real regression coverage.

## Ownership

The primary agent owns .github/workflows/ci.yml, renovate.json, vpnd/Cargo.toml and src/commands/{deploy,preflight,fleet}.rs test seams, and the new/updated test files.

## Execution

- [x] CIC-1787469521364147 Replace bare go install of gitleaks and actionlint with pinned release archives verified by sha256 against official checksums (mirroring the zizmor pattern); contract tests updated in tests/unit/test_gitleaks_rules.py #bug !high @item:CIC-1787463116104727
- [x] CIC-1787469521365899 Add renovate customManagers for the naive role's xcaddy and caddy pins; transport-binary pins in SOPS stay deliberately unmanaged per tests/unit/test_renovate_managers.py #bug !high @item:CIC-1787463116104727
- [x] CIC-1787469521366469 Add molecule-full-stack as a required CI job - the only surface exercising site.yml role ordering #bug !high @item:CIC-1787463116104727
- [x] CIC-1787469521367928 Swap vpnd's abandoned serde_yaml 0.9 for the maintained serde_yaml_ng fork; cargo test and clippy green #bug !high @item:CIC-1787463116104727
- [x] CIC-1787469521368079 Extend coverage: vpnd deploy pipeline order + conditional flags, preflight guard selection, fleet rotate flag mapping (unit); promote-spare OTP gate incl. replay/expiry/burn protection and diff-secrets fail-closed gates (integration stubs); provider listeners.tf parity contract. reconverge remains registry-dependent and uncovered by design #test !high @item:CIC-1787463116104727

## Verification

Observed evidence is recorded per step; completion advances the portfolio record at most to `review` until remote CI is observed on a pushed final SHA.
