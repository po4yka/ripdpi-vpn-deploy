# CIC-1787849827002217: Allocate execution steps through the public taskctl CLI

## Objective

Allocate execution steps through the public taskctl CLI

## Ownership

Implementation agent: `scripts/tasks/taskctl.py`,
`tests/unit/test_task_contract_workflow.py`, and existing CLI usage documentation.
Root agent: task records, final checks, review, and Git integration.

## Execution

- [x] CIC-1787849827020051 Define implementation and verification #bug !high @item:CIC-1787849827002217

## Verification

Source commit: `dd78d6bc795dedb174d7e9a91ac7707c5f37cb85`.
The root observed 86 focused tests plus four subtests passing, strict task
validation, and staged secret scanning. Independent review found and verified
fixes for Unicode line separators and multiline portfolio-title injection.
Two public CLI additions using the pinned OpenSpec tool passed before evidence
authoring; ordinary validation continued rejecting the absent verification.
The root then observed the full unit suite: 1086 passed and one existing
network-scanner skip. The consuming task independently used the committed CLI
to allocate six steps and passed strict OpenSpec and portfolio validation.
Hosted checks remain a protected-branch delivery gate; no runtime task is closed
by this tooling result.
