# CIC-1788630270264106: Restore vpnd release SBOM generation

## Objective

Restore vpnd release SBOM generation

## Ownership

One writer owns the shared SBOM action, release/CI wiring, regression coverage,
related documentation and this task. The original main checkout and its
unrelated merge state are preserved; external publication is not part of this fix.

## Execution

- [x] CIC-1788630270293392 Define implementation and verification #bug !high @item:CIC-1788630270264106
- [ ] CIC-1788630727376422 Restore release SBOM generation and verify real output, error paths and hosted delivery #bug !high @item:CIC-1788630270264106

## Verification

- Reproduced the previous missing-file failure with the real example emitter:
  SBOM_LABEL=vpnd-v1.3.0 still wrote sbom/example.json.
- Real cargo-cyclonedx 0.5.9 generated vpnd 1.3.0 inventory with 228 components
  and 229 dependency nodes, without changing Cargo.lock or reading secrets.
- The emitted artifact validates against the official CycloneDX 1.5 JSON schema;
  development-only insta and proptest are absent.
- Nineteen targeted tests and actionlint, ShellCheck, yamllint, zizmor and task
  contract validation pass. Hosted generation and protected-main delivery
  remain the final evidence boundary; no public release is created by this fix.
