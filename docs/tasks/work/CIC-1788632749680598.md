# CIC-1788632749680598: Fail real-VPS deploy when selected templates are missing

## Objective

Fail real-VPS deploy when selected templates are missing

## Ownership

One writer owns real-vps-deploy.yml, its status regression tests, related
CI documentation and this task in a dedicated worktree. The original main
checkout and unrelated merge state remain untouched. No provider operations
or repository configuration changes are part of this repair.

## Execution

- [x] CIC-1788632749698510 Define implementation and verification #bug !high @item:CIC-1788632749680598
- [ ] CIC-1788632841651963 Repair selected deploy status and verify regression, workflow contracts and hosted delivery #bug !high @item:CIC-1788632749680598

## Verification

- Hosted run 33390196436 reported both distros successful with Deploy and Destroy
  skipped; the actual former preflight shell also returned zero on empty templates.
- Regression tests first failed in four cases on the old workflow. After the fix,
  91 targeted tests pass, covering template selection, missing configuration,
  protected-environment contracts, cleanup and documentation counts.
- actionlint (including ShellCheck), yamllint, zizmor and task contracts pass.
- The live read-only environment check confirms a required reviewer remains
  configured. Required PR CI and protected-main delivery are the final boundary.
- No provider resource was created and no repository variable or secret was changed.
  These checks establish truthful CI status, not live deployment convergence.
