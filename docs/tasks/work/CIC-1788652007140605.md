# CIC-1788652007140605: Close automatic vpnd release handoff

## Objective

Close automatic vpnd release handoff

## Ownership

Codex owns release-please.yml, its version-update config, release-chain tests, release documentation and this task. Shared main integration is serialized with the active Wave A task; no other worktree is edited.

## Execution

- [x] CIC-1788652007161384 Define implementation and verification #bug !high @item:CIC-1788652007140605
- [x] CIC-1788652171920461 Review delivery evidence and complete task lifecycle #bug !high @item:CIC-1788652007140605

## Implementation and verification

- Removed the implicit disabled state and informational-error suppression.
  Explicit `false` still pauses automation; dispatch is restricted to a newly
  created root release, with Actions write permission isolated to the handoff.
- Kept the existing release workflow and its tag/SHA validation. The handoff
  validates the release outputs, then dispatches on the tag rather than main.
- Observed red: all 8 new regression cases failed on the original workflow.
  Observed green: 19 release/tag/SBOM cases and 22 governance/required-workflow
  cases passed; actionlint, yamllint and strict offline zizmor passed.
- The executable handoff tests use real Git tags and stub only the GitHub CLI.
  No tag or release was published for testing. Actual publication remains
  observable only after a reviewed release PR is merged.
- Found and repaired the next release blocker: the version updater omitted
  Cargo.lock. The real release-please 17.6.0 updater from the pinned action
  failed on the old configuration, then updated both files to a disposable
  candidate version with all dependency entries unchanged. Cargo metadata
  accepted that candidate with `--locked --offline`; 42 targeted tests pass.
- Resolved the review finding about release-PR checks by explicitly dispatching
  the existing CI and CodeQL workflows on the generated root release branch.
  CodeQL now accepts workflow_dispatch. No token secret, protection change or
  synthetic check result is used. Six additional executable/contract regressions
  failed before this fix; all 48 targeted cases now pass.

- Live follow-up corrected the PR-check strategy: explicit dispatch runs passed
  but were excluded from the protected PR rollup while its native events awaited
  approval. Removed those redundant runs and kept GitHub's native approval gate,
  now reported in the action summary and operator documentation. A repository
  writer approves the bot PR workflows before normal required CI and merge.
  The post-merge release tag/SHA handoff and lockfile fix remain unchanged.
- Protected `main` at `ea94beefa94afd5a11560547db128debd152bf27`
  passed exact-SHA CI run 34024059323 and release-please run 34024059176.
  Release-please completed and reported the approval boundary; binary dispatch
  was correctly skipped because that push did not create a new root release.
  No release or publication was simulated for closure.
