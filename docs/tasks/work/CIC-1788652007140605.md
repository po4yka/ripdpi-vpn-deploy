# CIC-1788652007140605: Close automatic vpnd release handoff

## Objective

Close automatic vpnd release handoff

## Ownership

Codex owns release-please.yml, release-chain tests, release documentation and this task. Shared main integration is serialized with the active Wave A task; no other worktree is edited.

## Execution

- [x] CIC-1788652007161384 Define implementation and verification #bug !high @item:CIC-1788652007140605
- [ ] CIC-1788652171920461 Review delivery evidence and complete task lifecycle #bug !high @item:CIC-1788652007140605

## Implementation and verification

- Removed the implicit disabled state and informational-error suppression.
  Explicit `false` still pauses automation; dispatch is restricted to a newly
  created root release, with Actions write permission isolated to the handoff.
- Kept the existing release workflow and its tag/SHA validation. The handoff
  validates the release outputs, then dispatches on the tag rather than main.
- Observed red: all 8 new regression cases failed on the original workflow.
  Observed green: 19 release/tag/SBOM cases and 22 governance/required-workflow
  cases passed; actionlint, yamllint and strict offline zizmor passed.
- The executable handoff tests use real Git tags and stub only the GitHub CLI.
  No tag or release was published for testing. Actual publication remains
  observable only after a reviewed release PR is merged.
