# CIC-1788668056138767: Verify shared Xray CI validator integrity

## Objective

Verify shared Xray CI validator integrity

## Ownership

Codex owns the shared Xray CI action, its two workflow consumers, focused tests,
testing documentation and this task. The existing conflicted main checkout is
untouched; implementation uses the dedicated integrity-fix worktree.

## Execution

- [x] CIC-1788668056157631 Define implementation and verification #bug !high @item:CIC-1788668056138767
- [ ] CIC-1788668297993096 Review hosted delivery and complete task lifecycle #bug !high @item:CIC-1788668056138767

## Verification

- Observed red: all six new installer/consumer cases failed before the shared
  action existed. After implementation, all 36 focused Xray tests passed.
- Executable tests use real ZIP extraction and SHA-256 verification with a local
  archive fixture; only download and privileged installation are redirected.
  Corruption, download failure and version drift stop before binary execution;
  runtime failure preserves the previously installed binary.
- Downloaded the official v26.3.27 Linux amd64 ZIP (21,136,402 bytes); its computed
  SHA-256 matched the shared pin and the upstream release API asset digest.
- Actionlint and the repository's strict offline zizmor gate passed.
- Full local `build-gate -- make check` was blocked by the uncached UpCloud
  Terraform provider 5.43.0. The generated task board was refreshed and task
  validation passed. Hosted validator execution remains the delivery gate.
- The first hosted template run verified the archive and executed Xray but
  exposed missing adjacent geodata after installation. The installer now copies
  both bundled data files with the runtime; the executable positive test failed
  before that repair and verifies all three installed files afterward.
