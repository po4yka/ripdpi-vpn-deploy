# CIC-1788672821616865: Balance portable pytest across four CI groups

## Objective

Balance portable pytest across four CI groups

## Ownership

Codex owns pytest dependencies, Make targets, CI unit/aggregate jobs, the shard report hook and verifier, focused tests, duration data and testing documentation. Work uses a dedicated worktree; the existing conflicted main checkout is preserved.

## Execution

- [x] CIC-1788672821634194 Define implementation and verification #chore !high @item:CIC-1788672821616865

## Implementation and verification

- Four `least_duration` pytest-split groups retain the existing required status via a full-coverage aggregate; local portable and native selection remains intact.
- 31 targeted grouping, coverage, workflow integrity and documentation tests passed. Runtime regressions exercise actual pytest-split selection, failures, skips and nonexecution.
- actionlint, strict zizmor, YAML lint, task validation and cloud-init schema passed locally.
- `build-gate -- make check` was attempted. Terraform validation is blocked locally by an uncached UpCloud 5.43.0 package; hosted validation is required before delivery. The default Docker socket was absent; the cloud-init schema subsequently passed using the running Colima context.
- Baseline hosted CI 34014412139 passed on d10fb672: 3975 portable tests, 4 native tests deselected, 1247.69 seconds. The complete measured seed is committed as gzip JSON.
- The real current collection contains 3992 portable tests. pytest-split assigns 998 tests per group with estimated measured durations of 311.38 seconds each; these are estimates, not observed grouped runtimes.
- Hosted four-group runtime, aggregate coverage and protected-main delivery remain to be verified.
- [ ] CIC-1788674474730972 Verify hosted group runtimes and deliver to protected main #chore !high @item:CIC-1788672821616865
