# CIC-1788690361800575: Bound CI runtime and cancel superseded auxiliary PR checks

## Objective

Bound CI runtime and cancel superseded auxiliary PR checks

## Ownership

Codex owns timeout fields in ci.yml, _rust.yml, tf-policy.yml, image-scan.yml,
contract-sync.yml, codeql.yml, markdown-link-check.yml and claude-md-touch.yml;
the last two also receive PR-scoped concurrency. Own related testing docs and
this task. Work occurs in the pr-workflow-timeouts worktree. This is scheduling
resource control only; required contexts, security checks and runtime deployment
contracts remain intact.

## Execution

- [x] CIC-1788690361818535 Define implementation and verification #chore !high @item:CIC-1788690361800575
- [ ] CIC-1788690471358858 Deliver protected main and verify runtime bounds and PR supersession #chore !high @item:CIC-1788690361800575

## Verification

- Five recent full hosted executions: maximum pytest job 409 seconds, Molecule
  failure/full-stack 389 seconds, role jobs 199 seconds, Rust tests 152 seconds.
  These observations include setup/execution and exclude runner queue time;
  they are a small warm-cache sample, not a cold-build guarantee.
- All 35 concrete templates across the standard CI graph and auxiliary checks
  have positive bounds. Existing limits and all other job fields match base.
- Actionlint and strict offline zizmor passed; 74 related scheduling, aggregate
  gate and executable-coverage regressions passed.
- Protected delivery, supersession observations and exact-main CI are pending.
