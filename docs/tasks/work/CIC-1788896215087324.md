# CIC-1788896215087324: Resolve symbolic base refs in deleted task history validation

## Objective

Resolve symbolic base refs before deleted task history is indexed and audited,
so documented base-aware validation works after a committed OpenSpec purge.

## Ownership

Primary owns `scripts/tasks/taskctl.py`, the existing history regression suite
in `scripts/tests/test_taskctl.py`, its collected-test count in
`docs/TESTING.md`, and this task's portfolio artifacts. The already committed
purge of CIC-1788741692326070 remains a separate lifecycle commit; no runtime,
provider, fleet, secret, or external acceptance paths are owned by this fix.

## Execution

- [x] CIC-1788896215095980 Resolve symbolic base refs and verify committed deletion history #bug @item:CIC-1788896215087324
