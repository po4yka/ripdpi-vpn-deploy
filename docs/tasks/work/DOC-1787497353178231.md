# DOC-1787497353178231: Correct documented-but-absent controls and stale docs

## Objective

Documentation stops claiming controls that do not exist and describes hazards where implementation fixes are deferred to sibling changes: the firewall geo-block claim is corrected, the naive credential-delivery pitfall matches reality, the cascade scaffold routing hazard is documented until its code fix lands, the canonical decoy-site tree is recorded, and the mirror snapshot-path contract is pinned in prose.

## Ownership

The primary agent owns ansible/roles/firewall/CLAUDE.md, ansible/role-tiers.yml comment block, ansible/roles/naive/AGENTS.md, ansible/roles/cascade-ingress/AGENTS.md, decoy-site notes in ansible/roles/nginx-xhttp and ansible/roles/reality-self-steal docs, and ansible/roles/subscription-host/AGENTS.md mirror section. Code behavior is out of scope: runtime fixes belong to their OpenSpec-backed sibling changes (SEC-1787496747898735, TST-1787497001212692, ANS-1787497148207353).

## Execution

- [ ] DOC-1787496118907223 Correct the geo-block claim in firewall CLAUDE.md and the role-tiers.yml comment: either remove the vpn.geo_block description or restate it as a documented-not-implemented control pending a build decision #bug !low @item:DOC-1787497353178231
- [ ] DOC-1787496118906782 Rewrite the naive AGENTS.md pitfall to describe actual credential delivery (secrets inline in a 0640 root:naive Caddyfile, log-suppressed render), removing the env-delivery claim #bug !low @item:DOC-1787497353178231
- [ ] DOC-1787496118907356 Document the cascade-ingress scaffold routing hazard (default-route AllowedIPs without Table = off) and the egress forwarding gap as explicit pitfalls pointing at the consolidation change for the code fix #bug !low @item:DOC-1787497353178231
- [ ] DOC-1787496118906791 Record which public-site tree is canonical for new decoy surfaces and cross-reference the content-hash clusterability warning in both role docs #docs !low @item:DOC-1787497353178231
- [ ] DOC-1787496118906908 Pin the expected mirror snapshot layout (payload tree rooted so sub/ lands at DEST root) in subscription-host docs ahead of the restore-layout assertion #docs !low @item:DOC-1787497353178231
- [ ] DOC-1787497353197117 Proofread pass: grep for remaining documented-but-absent control names across role docs; run make ci-fast to confirm no gate regressions #test !low @item:DOC-1787497353178231

## Verification

Observed evidence is recorded per step; documentation-only scope means local gates (`make ci-fast`, reference sanity) plus review are the acceptance surface.
