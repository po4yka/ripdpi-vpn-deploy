# OPS-1787495859957242: Refuse canary deploys scoped to non-canary secrets files

## Objective

Refuse canary deploys scoped to non-canary secrets files

## Ownership

The primary agent owns this focused change and its regression tests; shared paths are serialized with the vpnd lane.

## Execution

- [x] OPS-1787495859975511 Define implementation and verification #bug !high @item:OPS-1787495859957242

## Verified implementation

Canary gate tests accept only the documented canary secrets basenames and prove rejected paths do not invoke deployment. Implementation commit: `c8af1ed`.

Implementation verified at 1221ccb59ae90f4d5d7fc3951018dcbef1634841: local make check passed (1055 pytest, one pre-existing network-scan skip, 55 Bats, 79 Terraform tests, 45 Conftest tests, 102 snapshots); hosted CI run 33079404315 passed all 51 jobs. Probe schema synchronization is a separate withheld task.
