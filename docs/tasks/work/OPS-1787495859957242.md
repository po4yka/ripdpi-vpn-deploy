# OPS-1787495859957242: Refuse canary deploys scoped to non-canary secrets files

## Objective

Refuse canary deploys scoped to non-canary secrets files

## Ownership

Primary agent owns: Makefile; tests/unit/test_make_strict_gates.py. Shared files are serialized; sub-agents do not edit these paths.

## Execution

- [x] OPS-1787495859975511 Define implementation and verification #bug !high @item:OPS-1787495859957242

## Observed targeted verification

Production basenames, including one beneath a canary-named directory, fail before child deployment. Both canonical canary names reach the child command in the isolated test harness. Full union gate and final committed-source verification remain pending.
