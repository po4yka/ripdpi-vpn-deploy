# OPS-1787495860232652: Triage tier-2 and tier-3 findings from 2026-08-23 plumbing audit

## Objective

Triage tier-2 and tier-3 findings from 2026-08-23 plumbing audit

## Ownership

Primary owns `plans/013-018-*.md`, the `plans/README.md` triage updates, and
this task's portfolio artifacts. The docs/tasks/README.md provenance note is
owned by plan 014 execution, not by this task. No runtime, provider, fleet,
or secret paths are touched: this is a research task; implementation is
future work under the created plans.

## Decision summary (2026-09-09, at commit 15267007)

Planned — new handoff plans under `plans/`:

- decrypt-secrets.sh zero coverage → plan 013 (P1/S): bats suite over every
  guard of the secrets plaintext cache.
- taskctl negative-path test harness → plan 014 (P2/M): absorbs plan 005
  inline probes as permanent regression rows; folds TASKING-07 (document the
  receipts-provenance boundary in docs/tasks/README.md).
- blue-green/fleet-rotate happy-path tests → plan 015 (P2/M): stub-driven
  dry-run, plan validation, approval gate, and resume coverage.
- shellcheck gate for root-side .j2 templates → plan 016 (P2/S): extends the
  existing render-check harness to lint rendered shell output.
- local validate without terraform init + galaxy collections local install +
  check-prereqs expansion → plan 017 (P3/S): one local-parity bundle
  (shared plugin cache, `galaxy-install`, expanded prereq inventory).
- tool-pinning consistency → plan 018 (P3/S): single literal source for
  terraform version across ci.yml jobs plus a mise drift check.

Rejected with rationale (plans/README.md, "Findings considered and
rejected"):

- UTF-8 encoding sweep (~40 sites) — deferred: ASCII data domain, no
  triggering defect; revisit on the first real encoding bug.
- exception-root local validation — inert governance-gated scaffold; shell
  scripts already inside `make shellcheck`.
- ci-fast parallelization — hosted CI already parallel; the local serial
  union preserves ordering between steps that share generated state.

Tier-3 polish:

- TASKING-07 (receipts prove consistency, not provenance) — folded into
  plan 014.
- BASHCORRECT-01 residual array-hygiene item — stays rejected (existing
  plans/README.md entry).

## Execution

- [x] OPS-1787495860255193 Define implementation and verification #research !low @item:OPS-1787495860232652
