# SCR-1787463115839994: Fix medium-priority script findings from audit

## Objective

Three medium-priority script findings fixed with regression coverage: ntfy alerts survive macOS bash 3.2 under set -u, the pre-deploy secrets gate diagnoses instead of crashing, and the listener collision guard sees range-range overlaps.

## Ownership

The primary agent owns scripts/warm-spare-watcher.sh, scripts/spot-check-secrets.py, scripts/check-listener-collisions.py and their test files.

## Execution

- [x] SCR-1787469521355351 warm-spare-watcher: expand the possibly-empty ntfy auth array with the bash-3.2-safe ${auth[@]+...} form at both call sites so set -u cannot kill the alert push #bug !high @item:SCR-1787463115839994
- [x] SCR-1787469521356206 spot-check-secrets: diagnose malformed YAML (exit 2, no traceback, no content echo), guard openssl CN parsing so CN-less certs neither crash nor false-positive as self-signed; covered by tests/unit/test_spot_check_secrets.py #bug !high @item:SCR-1787463115839994
- [x] SCR-1787469521357328 check-listener-collisions: add range-vs-range overlap detection with allowlist suppression; covered by three new cases in tests/unit/test_listener_collision_guard.py #bug !high @item:SCR-1787463115839994
- [x] SCR-1787469521358077 Run named gates: shellcheck -S warning, focused pytest files, full pytest tests/unit, make validate #test !high @item:SCR-1787463115839994

## Verification

Observed evidence is recorded per step; completion advances the portfolio record at most to `review` until remote CI is observed on a pushed final SHA.
