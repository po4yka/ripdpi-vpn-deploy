# TST-1787497584762133: Close vpnd audit test coverage gaps

## Objective

Every vacuous or bypassed test from the audit becomes real coverage of production behavior, and requirement-shaped properties replace implementation-shaped ones.

## Ownership

The primary agent owns vpnd/tests/* additions and the touched inline #[cfg(test)] modules; coordinates with VPD remediation tasks to avoid duplicate fixtures.

## Execution

- [x] TST-1787497792830773 Replace vacuous update_cache tests with tests calling the real cache read/write/expiry and notice functions #test !high @item:TST-1787497584762133
- [x] TST-1787497792825316 Rewrite doctor_bundle redaction tests against the production redact_secrets and remove the triple escape hatches; extend proptest_redact to requirement shape over runtime-dir variants #test !high @item:TST-1787497584762133
- [x] TST-1787497792828767 Cover Registry::save/load and Cmd run/capture execution paths (nonzero exit, missing binary, signal death) #test !high @item:TST-1787497584762133
- [x] TST-1787497792833647 Add rejection-branch tests for validate_config/validate_profiles, windows() recovery, parse_duration units, empty/malformed YAML secrets, validate_token edges, and the fingerprint-secrecy property #test !high @item:TST-1787497584762133

## Verification

Observed evidence is recorded per step; completion advances the portfolio record at most to `review` until remote CI is observed on a pushed final SHA.

## Accepted evidence

Candidate `66f08e9ca7f205be27bc7763dbaf02227b04d1f7` passed all 169 Rust release tests and formatting; hosted CI run 33081754654 passed, including strict Clippy, dependency policy, MSRV, and the complete regression suite. All seven associated PR workflows passed. Real command outcomes, malformed private JSON, input rejection, duration units, baseline Blocked-to-Ok recovery, and fingerprint secrecy are covered.

The new probe schema, zero-duration policy, cancellation and durable-session behavior remain withheld under VPD-1787497252303967. Audit coverage does not claim those deferred fixes. The repeated local union gate reached a RustSec advisory database network-fetch failure; its security check was not disabled or weakened, and the hosted dependency check passed.
