# VPD-1787497584287174: Fix update-check version comparison and add network timeout

## Objective

The update notice fires only for genuinely newer vpnd releases across both release trains, and a stalled network cannot hang the command.

## Ownership

The primary agent owns vpnd/src/commands/update.rs, tests/update_cache.rs or a successor file, and this work file.

## Execution

- [ ] VPD-1787497792811760 Replace string inequality with proper version ordering tolerant of the vpnd-v prefix and repo-level tags; cover newer/older/equal and cross-train cases in tests #bug @item:VPD-1787497584287174
- [ ] VPD-1787497792813037 Add an explicit timeout to the GitHub fetch so a stalled connection fails fast while the command still exits 0; test with a stubbed slow endpoint #bug @item:VPD-1787497584287174

## Verification

Observed evidence is recorded per step; completion advances the portfolio record at most to `review` until remote CI is observed on a pushed final SHA.
