# VPD-1787497584429255: Make host registry persistence atomic

## Objective

Registry writes use temp+rename like every other persistence site in vpnd; a crash mid-write cannot destroy hosts.toml.

## Ownership

The primary agent owns vpnd/src/state/registry.rs, its new tests, and this work file.

## Execution

- [ ] VPD-1787497792815266 Rewrite Registry::save to temp+rename and add tests for crash-safety semantics plus corrupt-file load error reporting #bug @item:VPD-1787497584429255

## Verification

Observed evidence is recorded per step; completion advances the portfolio record at most to `review` until remote CI is observed on a pushed final SHA.
