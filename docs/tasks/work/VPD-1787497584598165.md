# VPD-1787497584598165: Move blocking IO out of async contexts in vpnd

## Objective

No std subprocess spawns or heavy file IO execute directly on tokio worker threads.

## Ownership

The primary agent owns vpnd/src/secrets.rs, vpnd/src/commands/{ai_docs,probe_matrix}.rs, vpnd/src/pages/qr.rs call sites, and this work file.

## Execution

- [ ] VPD-1787497792818065 Resolve uid without a subprocess where feasible (or via a single cached spawn_blocking helper) replacing both id -u sites; keep gate semantics identical #chore !low @item:VPD-1787497584598165
- [ ] VPD-1787497792820170 Wrap ai-docs bulk emit, probe-matrix report write, bundle gzip, and share file IO in spawn_blocking; cargo clippy -D warnings green #chore !low @item:VPD-1787497584598165

## Verification

Observed evidence is recorded per step; completion advances the portfolio record at most to `review` until remote CI is observed on a pushed final SHA.
