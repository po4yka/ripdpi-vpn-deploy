ansible-core==2.21.0
ansible-lint==26.4.0
jmespath==1.1.0
molecule==26.4.0
molecule-plugins[docker]==25.8.12
yamllint==1.38.0

# Used by validators in CI (scripts/check-secrets-coverage.py +
# scripts/check-templates-render.py +
# scripts/validate-secrets.py — which uses the jsonschema pulled
# transitively by ansible-lint, no explicit pin here).
PyYAML==6.0.3
Jinja2==3.1.6
referencing<0.38
typing-extensions==4.15.0

# Operator-triggered reachability baselines; installed from the hash-pinned lock in CI.
rkn-block-checker==0.5.1

# Test-only; installed from the hash-pinned lock in CI.
pytest==9.1.1

# Developer tooling — install via `make install-hooks`.
# TODO: once pip-compile --generate-hashes has been run offline to regenerate
# requirements.txt, the Makefile install-hooks target should switch to
# `pip install -r requirements.txt` so pre-commit is hash-verified.
pre-commit

# Optional experimental pyinfra read-only host audit pilot is intentionally not part of the default hash-pinned toolchain while pyinfra depends on vulnerable Paramiko releases. Install pyinfra separately only when running `make pyinfra-audit`.
