destroy table inet cascade_ingress
table inet cascade_ingress {
  # Implementation-only scaffold. Activation must add only the classifier's
  # foreign-flow mark contract described in the tunnel configuration.
}
